fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
aiosqlite==0.19.0
asyncpg==0.29.0

# Pydantic
pydantic==2.5.2
pydantic-settings==2.1.0

# AI Libraries - Google Gemini for cost optimization
# Pin to a known-good version to avoid deep dependency resolution during builds
google-generativeai==0.8.6
google-ai-generativelanguage==0.6.15
httpx==0.25.2
openai==1.3.0

# Uploads / Forms
python-multipart==0.0.6

# WebSockets
websockets==12.0

# Utilities
python-dateutil==2.8.2

# Google + Outlook Integrations
# Pin google-api-core to avoid deep dependency resolution loops
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.2.0
google-api-python-client==2.108.0
google-api-core==2.28.1
msal==1.24.1

# HTTP
requests==2.31.0

# JWT Auth
python-jose[cryptography]==3.3.0

# Password Hashing
bcrypt==4.0.1
passlib[bcrypt,argon2]==1.7.4
argon2-cffi==23.1.0

python-dotenv==1.0.0
email-validator==2.1.0

# IMAP/SMTP Email
imapclient==2.3.1
aiosmtplib==3.0.1

# Document parsing (Phase 4 attachments/document analysis)
PyPDF2==3.0.1
python-docx==1.1.2
openpyxl==3.1.5
python-pptx==0.6.23

# Encryption - AES-256 for credential storage
cryptography==41.0.7

# Migrations
alembic==1.12.1

# Task Queue and Scheduling
celery==5.3.4
redis==5.0.1
python-dateutil==2.8.2

# LLM: Add Anthropic client (keeps backwards compatibility)
anthropic==0.7.4

# Pin protobuf and proto-plus to stable versions to avoid large source builds and OOM during image builds
# Updated proto-plus to satisfy google-ai-generativelanguage dependency (>=1.22.3)
proto-plus==1.22.3
protobuf==4.25.3

# Timezone handling
pytz>=2023.3
# (If you need timezone support, add zoneinfo or python-dateutil as required)

# Additional utilities
python-dotenv==1.0.0
pydantic-extra-types==2.3.0
stripe==13.1.2

# Testing (dev)
pytest==7.4.2
pytest-asyncio==0.23.3

